基金/ETF 行情可视化与量化分析系统 — 需求与架构文档

# **文档版本**: v0.1 (整合架构与API契约)

**状态**: 架构已定，待开发

**适用对象**: 个人投资辅助工具

## 功能需求

### 1.1 Phase 1 — 基础功能

#### 1.1.1 行情查询

- 输入 ETF/基金代码，支持回车或按钮触发查询。
- 查询过程显示加载状态；失败时明确提示（如"代码不存在"、"网络异常"）。
- 空输入时显示引导文案与示例代码。

#### 1.1.2 K 线图展示

- 展示标准 OHLCV K 线图；基金日线模式下展示净值折线图。
- 支持日线/周线切换（周线需严格按金融标准重采样：开=首日，收=末日，高/低=极值）。
- 支持鼠标滚轮缩放、滑块拖拽、十字准星及悬停数据显示。

#### 1.1.3 技术指标叠加

- 可显示 MA5、MA10、MA20、MA30、MA60 五条均线。
- 不同颜色区分（MA5 红、MA10 橙、MA20 黄、MA30 紫、MA60 蓝），图例支持点击隐藏/显示。
- MA 均线直接叠加在 K 线主图区域显示。
- 无成交量副图、无 MA 副图，保持界面简洁。
- MA 由后端预计算，前端零计算压力。
- 基准列：ETF（任意周期）与基金周线基于收盘价 `close`；开放式基金日线仅有单位净值，基于 `nav` 计算 MA。

#### 2.1.4 信息展示与降级

- 展示标的的基础信息卡片（代码、名称、类型、当前周期）。
- 数据源不可用时，以本地缓存降级展示，并前端提示"数据可能非最新"。

#### 2.1.5 数据增量更新

- 优先读取本地 DB；无数据拉取近 3 年全量，有数据仅拉取增量。
- 幂等写入，不产生重复记录。
- 数据源请求间隔可配置（默认 1.5 秒），防 IP 封禁。

### 2.2 Phase 2 — 工程质量

- **测试覆盖**：后端核心业务/API 集成测试，前端组件交互测试。
- **日志可观测性**：记录请求耗时/状态码，按级别输出到文件。
- **配置管理**：所有参数通过 .env 或配置文件管理，禁止硬编码。
- **错误处理**：无效代码、数据源异常、内部异常、前端渲染异常均有对应降级/提示策略。

## 系统架构与技术选型

采用 **前后端分离架构**，后端专注算力与数据获取，前端专注可视化与交互。

### 2.1 技术栈

- **后端**：Python 3.10+, FastAPI (异步API, 自动文档), Pydantic v2 (数据校验), Pandas (数据计算), Loguru (日志)
- **前端**：Vue 3 (Composition API), Vite (构建工具), TailwindCSS (响应式UI), Pinia (状态管理)
- **图表库**：ECharts (Apache 可视化库，支持蜡烛图/折线/十字准星/缩放/MA叠加)
- **数据库**：SQLite (启用 WAL 模式支持读写并发，轻量零配置)

### 2.2 工程结构

采用 **Monorepo** 结构，适合个人项目与本地自用。

1.  quant-trading/
2.  ├── backend/ # 后端（uv 管理依赖）
3.  │ ├── app/
4.  │ │ ├── main.py # FastAPI 入口
5.  │ │ ├── config.py # 配置管理
6.  │ │ ├── api/ # 路由层 (v1/market.py)
7.  │ │ ├── core/ # 核心组件 (数据库初始化, 日志)
8.  │ │ ├── services/ # 业务层 (增量获取, 重采样, MA计算)
9.  │ │ ├── models/ # 数据库模型
10. │ │ └── schemas/ # Pydantic 请求/响应模型
11. │ ├── tests/ # pytest 测试
12. │ ├── data/ # SQLite 数据库存放 (data.db)
13. │ ├── logs/ # 日志文件存放
14. │ └── pyproject.toml
15. ├── frontend/ # 前端
16. │ ├── src/
17. │ │ ├── components/ # 封装的 KlineChart 组件, UI 卡片
18. │ │ ├── views/ # 页面 (MarketView)
19. │ │ ├── api/ # Axios 封装及接口定义
20. │ │ └── stores/ # Pinia 状态管理
21. │ ├── package.json
22. │ └── vite.config.ts # 包含 /api 代理配置
23. ├── scripts/
24. │ └── run_dev.py # 一键启动前后端开发服务脚本
25. └── docs/

### 2.3 数据存储与计算模型

- **存储模型**：表 klines 采用 (code, date) 复合主键，利用 INSERT OR REPLACE 实现幂等写入。
- **计算模型**：SQLite 仅负责存取原始日线数据。周线重采样和 MA 计算通过 SQL 取出后在 **Pandas DataFrame (内存)** 中完成，保证计算逻辑的高效与易测试性。

## API 接口契约

### 3.1 核心接口：获取行情数据

**GET /api/v1/market/kline/{code}**

**路径参数**:

- code (string): 标的代码 (如 159915)

**查询参数**:

- type (string, 必填): 标的类型，可选 etf | fund。由前端搜索栏上方的 ETF/基金选项条指定，后端据此路由数据源，不再按代码前缀猜测。
- period (string, 默认 daily): 周期，可选 daily | weekly

**成功响应 (200 OK)**:

1.  {
2.  "instrument": {
3.  "code": "159915",
4.  "name": "创业板ETF",
5.  "type": "ETF"
6.  },
7.  "is_degraded": false,
8.  "klines": \[
9.  {
10. "date": "2024-01-02",
11. "open": 1.852, "high": 1.872, "low": 1.840, "close": 1.850, "volume": 12345600, "nav": null,
12. "ma5": 1.845, "ma10": 1.840, "ma20": 1.830, "ma30": 1.810, "ma60": null
13. }
14. \]
15. }

_注：前 59 天的 ma60 为 null，前端图表库遇到 null 自动断开线条。_

_注：ETF 行情返回 open/high/low/close/volume，`nav` 为 null；开放式基金日线仅返回 `nav`（单位净值），OHLCV 为 null，前端绘净值折线；基金周线由日净值按金融标准重采样为伪 K 线（开=首日净值、收=末日净值、高/低=区间极值、volume=null），`close` 字段承载该周末净值。_

### 3.2 数据源

- 行情数据源为 **akshare**（无需 token，覆盖 ETF 历史行情与开放式基金单位净值走势）。akshare 为同步阻塞库，后端用 `asyncio.to_thread` 包裹调用，避免阻塞事件循环。
- 所有数据源原始字段名映射隔离在 `app/services/datasource.py` 适配器内，上游只接触规范化后的 DataFrame。

### 3.2 统一错误响应

所有错误返回统一的 JSON 结构：

1.  {
2.  "detail": {
3.  "code": "INVALID_CODE",
4.  "message": "输入的代码不存在或无数据"
5.  }
6.  }

|     |     |     |     |
| --- | --- | --- | --- |
| **场景** | **HTTP 状态码** | **业务码 code** | **用户提示 message** |
| 代码不存在/无数据 | 404 | NOT_FOUND | 输入的代码不存在或无数据 |
| 数据源故障且本地无缓存 | 503 | DATA_SOURCE_ERROR | 数据源请求失败，且本地无缓存 |
| 参数校验错误 | 422 | VALIDATION_ERROR | 参数校验失败 |

## 前端展示要求（视觉与交互）

### 4.1 整体布局与主题

- **深色主题**：采用黑色或深灰色背景，符合金融交易软件的专业视觉习惯，减少眼部疲劳。
- **双栏布局**：
    - **左侧边栏**：包含搜索框，显示标的列表（代码、名称、当前价格、涨跌幅）。
    - **主内容区**：展示标的基础信息、 K 线图、技术指标。

### 4.2 左侧标的列表

- **搜索框**：
    - 可以查询指定代码的交易数据。通过按钮切换查询标的属于基金还是etf。
- **展示内容**：
    - 标的代码（如 159915）
    - 标的名称（如 创业板ETF）
    - 当前价格（如 1.852）
    - 涨跌幅（红色表示上涨，绿色表示下跌）
- **交互**：
    - 点击列表项，主内容区自动切换到对应标的的 K 线图。
    - 搜索历史列表默认显示最近搜索过的5条标的。可加星标移入星标列表。
    - 星标列表列表项可添加、删除或者手动排序。

### 4.3 主 K 线图区域

- **核心图表**：
    - 单 K 线图，蜡烛图（K 线）展示 OHLCV 数据。
    - MA5/10/20/30/60 移动平均线直接叠加在 K 线主图区域，颜色区分清晰。
    - 无成交量副图和 MA 副图。
- **交互功能**：
    - 鼠标悬停显示十字准星，显示对应时间点的价格、技术指标数据。
    - 支持鼠标滚轮缩放（放大/缩小时间范围）。
    - 支持拖拽平移（左右滑动查看历史数据）。
    - 支持点击图例隐藏/显示特定指标。

### 4.4 顶部控制栏

- **时间周期选择**：提供 1周和1日 选项，点击切换图表周期。
- **技术指标开关**：控制是否显示移动平均线及其他指标。
- **显示设置**：可能包含网格线、标记等开关。
- **价格信息**：显示标记价格、最新价格等关键数据。

### 4.5 响应式设计

- **桌面端**：采用双栏布局，充分利用屏幕空间。
- **移动端**：自动调整为上下布局（列表在上，图表在下），确保触摸操作友好。

## 工程化与部署方案

### 5.1 本地开发环境

- 通过执行 scripts/run_dev.py 脚本同时拉起后端 uvicorn (端口 8000) 和前端 vite (端口 5173)。
- 前端配置 Vite Proxy，将 /api 请求转发至后端，消除跨域问题。
- 支持 Ctrl+C 一键终止所有服务。

### 5.2 本地“生产”运行模式

- 执行 npm run build 将前端打包为静态文件至 frontend/dist。
- 后端 FastAPI 挂载 StaticFiles，直接托管前端静态资源。
- 最终仅需运行一条命令 uvicorn app.main:app --port 8000，浏览器访问 http://localhost:8000 即可获得前后端一体的单机应用。